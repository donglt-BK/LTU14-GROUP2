spring:
  datasource:
    url: jdbc:mysql://localhost:3306/olympia
    username: root
    password: 123456
  liquibase:
    enabled: true
    change-log: classpath:/liquibase/db-changelog.xml
    drop-first: true
server:
  port: 8109
