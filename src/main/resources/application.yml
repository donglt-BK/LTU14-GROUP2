spring:
  datasource:
    url: jdbc:mysql://localhost:3306/olympia
    username: root
    password: 12345678
  liquibase:
    enabled: true
    change-log: classpath:/liquibase/db-changelog.xml
    drop-first: false
server:
  port: 8109
